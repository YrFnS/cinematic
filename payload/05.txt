;comp.threshold.value=-19;comp.ratio.value=3;this.master.connect(comp).connect(ctx.destination);this.filter=ctx.createBiquadFilter();this.filter.type='lowpass';this.filter.frequency.value=600;this.filter.connect(this.master);this.os=[];[36.71,55,73.42,110].forEach((f,i)=>{let o=ctx.createOscillator(),g=ctx.createGain();o.type=i===0?'sine':i===3?'sine':'triangle';o.frequency.value=f;g.gain.value=[.18,.06,.03,.012][i];o.connect(g).connect(this.filter);o.start();this.os.push(o)});let buf=ctx.createBuffer(1,ctx.sampleRate*3,ctx.sampleRate),d=buf.getChannelData(0);for(let i=0;i<d.length;i++)d[i]=(Math.random()*2-1)*(1-i/d.length*.25);let noise=ctx.createBufferSource();noise.buffer=buf;noise.loop=true;this.nf=ctx.createBiquadFilter();this.nf.type='bandpass';this.nf.frequency.value=350;this.ng=ctx.createGain();this.ng.gain.value=.014;noise.connect(this.nf).connect(this.ng).connect(this.master);noise.start();let impulse=ctx.createBuffer(2,ctx.sampleRate*3.5,ctx.sampleRate);for(let c=0;c<2;c++){let x=impulse.getChannelData(c);for(let i=0;i<x.length;i++)x[i]=(Math.random()*2-1)*Math.pow(1-i/x.length,2.7)}let rev=ctx.createConvolver(),rg=ctx.createGain();rev.buffer=impulse;rg.gain.value=.16;this.filter.connect(rev).connect(rg).connect(this.master)}async toggle(){if(!this.ctx)this.create();if(this.ctx.state==='suspended')await this.ctx.resume();this.on=!this.on;this.master.gain.setTargetAtTime(this.on?.18:.0001,this.ctx.currentTime,.25);return this.on}update(p,v){if(!this.ctx)return;let t=this.ctx.currentTime;this.filter.frequency.setTargetAtTime(330+p*1550+Math.abs(v)*460,t,.18);this.nf.frequency.setTargetAtTime(260+p*950,t,.25);this.ng.gain.setTargetAtTime(.01+Math.abs(v)*.025+p*.008,t,.2);this.os.forEach((o,i)=>o.detune.setTargetAtTime(p*185+v*15+i*4,t,.25))}impact(i){if(!this.ctx||i===this.last)return;this.last=i;let t=this.ctx.currentTime,o=this.ctx.createOscillator(),g=this.ctx.createGain();o.frequency.setValueAtTime(86+i*8,t);o.frequency.exponentialRampToValueAtTime(26,t+1);g.gain.setValueAtTime(.1,t);g.gain.exponentialRampToValueAtTime(.0001,t+1.1);o.connect(g).connect(this.master);o.start(t);o.stop(t+1.2)}}
const chapters=[{at:0,label:'APPROACH',eye:'THE APPROACH',title:'THE TEMPLE<br>REMEMBERS.',body:'Move forward. The world will meet you halfway.'},{at:.27,label:'THRESHOLD',eye:'THE THRESHOLD',title:'CROSS<br>WITHOUT FEAR.',body:'Stone, water, and light align around your passage.'},{at:.52,label:'SANCTUM',eye:'THE SANCTUM',title:'THE CORE<br>IS LISTENING.',body:'Look around. The artifact responds before it is touched.'},{at:.76,label:'AWAKENING',eye:'THE AWAKENING',title:'MATTER<br>LEARNS TO BLOOM.',body:'The sealed form opens and the architecture releases you.'},{at:.94,label:'DAWN',eye:'THE DAWN',title:'LIGHT<br>RETURNS CHANGED.',body:'The path ends where the next world begins.'}];
const renderer=new Renderer(),score=new Score(),chapter=document.querySelector('.chapter'),num=document.querySelector('.num'),eye=document.querySelector('.eyebrow'),title=document.querySelector('.chapter h1'),desc=document.querySelector('.chapter .body'),tlc=document.querySelector('.timeline-chapter'),timecode=document.querySelector('.time'),rail=[...document.querySelectorAll('.rail button')],sound=document.querySelector('.sound'),soundLabel=document.querySelector('.sound .label'),replay=document.querySelector('.replay'),brand=document.querySelector('.brand');let target=.002,currentChapter=-1,drag=false,dragY=0,dragP=0,pid=null;
function chapterAt(p){let n=0;chapters.forEach((c,i)=>{if(p>=c.at-.03)n=i});return n}function setChapter(i){if(i===currentChapter)return;currentChapter=i;let c=chapters[i];chapter.classList.toggle('compact',i>0);chapter.classList.add('swap');setTimeout(()=>{num.textContent=String(i+1).padStart(2,'0');eye.textContent=c.eye;title.innerHTML=c.title;desc.textContent=c.body;tlc.textContent=c.label;rail.forEach((b,k)=>b.classList.toggle('active',k===i));score.impact(i);chapter.classList.remove('swap')},180)}function setProgress(p,v=0,instant=false){let prev=target;target=clamp(p);renderer.setProgress(target,v||(target-prev)*15,instant)}function ui(){let p=renderer.progress;document.documentElement.style.setProperty('--p',p.toFixed(5));let sec=Math.round(p*74);timecode.textContent=`${String(Math.floor(sec/60)).padStart(2,'0')}:${String(sec%60).padStart(2,'0')}`;setChapter(chapterAt(p));body.classList.toggle('is-finale',p>.955);score.update(p,renderer.velocity);requestAnimationFrame(ui)}
addEventListener('wheel',e=>{if(e.ctrlKey)return;e.preventDefault();let d=Math.sign(e.deltaY)*Math.min(Math.abs(e.deltaY),260);setProgress(target+d*.00055,d*.007)},{passive:false});addEventListener('pointerdown',e=>{if(e.target.closest('button,a'))return;drag=true;pid=e.pointerId;dragY=e.clientY;dragP=target;try{canvas.setPointerCapture?.(e.pointerId)}catch{}body.classList.add('dragging')});addEventListener('pointermove',e=>{if(e.pointerType!=='touch')renderer.setPointer(e.clientX/innerWidth*2-1,-(e.clientY/innerHeight*2-1));if(drag&&e.pointerId===pid){let d=(dragY-e.clientY)/Math.max(innerHeight,320);setProgress(dragP+d*(coarse?1.05:.72),d*1.8)}},{passive:true});function end(e){if(e.pointerId!==pid)return;drag=false;pid=null;body.classList.remove('dragging')}addEventListener('pointerup',end);addEventListener('pointercancel',end);addEventListener('pointerleave',e=>{if(!drag)renderer.setPointer(0,0);end(e)});addEventListener('keydown',e=>{if(['ArrowDown','ArrowRight','PageDown',' '].includes(e.key)){e.preventDefault();setProgress(target+.045,.8)}if(['ArrowUp','ArrowLeft','PageUp'].includes(e.key)){e.preventDefault();setProgress(target-.045,-.8)}if(e.key==='Home'){e.preventDefault();setProgress(0,0,true)}if(e.key==='End'){e.preventDefault();setProgress(1,0,true)}if(e.key.toLowerCase()==='m'&&!e.metaKey&&!e.ctrlKey){e.preventDefault();sound.click()}});rail.forEach(b=>b.addEventListener('click',()=>setProgress(+b.dataset.p)));replay.addEventListener('click',()=>setProgress(0));brand.addEventListener('click',e=>{e.preventDefault();setProgress(0)});sound.addEventListener('click',async()=>{try{let on=await score.toggle();sound.setAttribute('aria-pressed',String(on));soundLabel.textContent=on?'SOUND / ON':'SOUND / OFF';score.impact(currentChapter)}catch(e){console.warn('[SENTINEL audio]',e);soundLabel.textContent='SOUND / N/A'}});
window.__SENTINEL={setProgress:(p,instant=true)=>setProgress(p,0,instant),setPointer:(x,y)=>renderer.setPointer(x,y),pause:()=>{renderer.running=false},resume:()=>{renderer.running=true;renderer.last=performance.now()},getState:()=>({target,progress:renderer.progress,fps:renderer.fps,webgl:!!renderer.gl,renderer:body.dataset.renderer||'',canvas:[canvas.width,canvas.height],chapter:currentChapter,body:body.className})};setTimeout(()=>renderer.reveal(),3200);requestAnimationFrame(ui);
})();
</script>
</body>
</html>
